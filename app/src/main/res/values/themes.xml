<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FlexMeals" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/colorPrimary</item>
    </style>
    <style name="Theme.FlexMeals" parent="Base.Theme.FlexMeals" />

    <style name="SplashTheme" parent="Base.Theme.FlexMeals">
        <item name="colorPrimary">@color/colorPrimaryVariant</item>
    </style>

    <style name="MainSplash" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/white</item>
    </style>

    <style name="ButtonSplash" parent="Base.Theme.FlexMeals">
        <item name="backgroundTint">@color/colorPrimary</item>
        <item name="android:paddingVertical">15dp</item>
        <item name="textFillColor">@color/white</item>
        <item name="android:textSize">20sp</item>
        <item name="android:background">@drawable/button_corner</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="CustomTextAppearance" parent="TextAppearance.MaterialComponents.Body1">
        <item name="android:textSize">18sp</item> <!-- Adjust text size -->
        <item name="android:textColor">@color/colorText</item> <!-- Set text color -->
    </style>

    <style name="MyDatePickerDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="CustomDatePickerDialog" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <item name="background">@color/colorBackgroundLight</item>
        <item name="colorAccent">@color/colorAccent1</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@color/colorText</item>
    </style>


</resources>